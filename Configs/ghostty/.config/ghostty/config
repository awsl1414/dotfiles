# --- Ghostty 1.3.0  ---
# 更新日期：2026-03-10

# --- 基础视觉与主题 ---
theme = "Catppuccin Macchiato"

# 原生滚动条（1.3.0 已完美支持）
scrollbar = "system"

# 现代模糊渲染与透明度（1.3.0 升级为 macOS 原生液态玻璃效果，Linux 自动忽略）
background-opacity = 0.9
# 推荐值：macos-glass-regular / macos-glass-clear
background-blur = "macos-glass-regular"

# --- 字体配置 ---
font-family = "Maple Mono NF CN"
font-family-italic = "Maple Mono NF CN Italic"
font-family-bold-italic = "Maple Mono NF CN Bold Italic"

font-synthetic-style = false
font-feature = "calt,liga,-dlig"

# --- 光标与交互 ---
cursor-style = "underline"
cursor-style-blink = true

cursor-color = "cell-foreground"
cursor-text = "cell-background"
adjust-cursor-thickness = 2

# 交互行为
copy-on-select = true
# 1.3.0 增强：输出时也自动滚动到底
scroll-to-bottom = "keystroke,output"
confirm-close-surface = false
focus-follows-mouse = true
mouse-hide-while-typing = true

# 1.3.0 新增：最小对比度保护（防止浅色主题文字隐形）
minimum-contrast = 4.5

# --- 窗口布局优化 ---
window-padding-x = 10
window-padding-y = 10
window-padding-balance = true
window-padding-color = "extend"

# 1.3.0 新增：分屏缩放保持 + 工作目录继承
split-preserve-zoom = "navigation"
window-inherit-working-directory = true
tab-inherit-working-directory = true
split-inherit-working-directory = true

# --- 系统集成与优化 ---
shell-integration = "zsh"
shell-integration-features = "no-sudo,cursor,title,ssh-env,ssh-terminfo"

window-subtitle = "working-directory"
image-storage-limit = 335544320

# 1.3.0 重磅新特性：命令完成桌面通知（长命令如 build、deploy 后自动提醒）
notify-on-command-finish = unfocused
notify-on-command-finish-action = no-bell,notify
notify-on-command-finish-after = 30s

# --- macOS 平台专项 ---
macos-titlebar-style = "tabs"
macos-option-as-alt = "left"
macos-non-native-fullscreen = true
macos-window-shadow = true
macos-icon = "official"

# --- Linux 平台专项（Bavaria 用户推荐）---
window-decoration = true
linux-cgroup = "always"

# --- 1.3.0 快捷键与 Quick Terminal ---
keybind = "global:cmd+shift+g=toggle_quick_terminal"
quick-terminal-position = "top"
quick-terminal-animation-duration = 0.2

# 生产力快捷键
keybind = "cmd+f=start_search"
keybind = "cmd+d=new_split:right"
keybind = "cmd+shift+d=new_split:down"
keybind = "cmd+z=toggle_split_zoom"

# 标签页跳转
keybind = "cmd+1=goto_tab:1"
keybind = "cmd+2=goto_tab:2"
keybind = "cmd+3=goto_tab:3"